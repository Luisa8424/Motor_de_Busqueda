class Departamento:
    CodDepartamento: str
    NomDepartamento: str
    Coordinador: Usuario
    
        
    
def __init__(self, facultad):
        self.Facultad = Facultad()
        
def BuscarDepartamento(self,codDepartamento:str, nombreDepartamento: str):
         """Mostrar """
         return codDepartamento:str, nombreDepartamento: str
def Definir(self):
         """Mostrar """
         return Departamento
     
   f = open("usuarios.txt", "w")
   f.write(codDepartamento:str, nombreDepartamento: str)
   87
   f.close()
   f=open("usuarios.txt", "a")
   f.write("agregar codDepartamento, nombreDepartamento")
   43
   f.close()
   f = open('usuarios.txt', 'r')
   lines = f.readlines()
   
   for i in range(len(lines)):
       lines[i] = lines[i].replace('\n', '')
       lista_codDepartamento_nombreDepartamento = []
   for i in lines:
       lista_codDepartamento_nombreDepartamento.append(i.split(" "))
       publicaciones = []
   for i in lista_codDepartamento_nombreDepartamento:
       Departamento.append(i[0])
       Departamento.sort()
       print(Departamento)
       f.close()
       
       
